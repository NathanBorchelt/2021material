import { SelectOnlyOptionDirective } from './select-only-option.directive';

describe('SelectOnlyOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectOnlyOptionDirective();
    expect(directive).toBeTruthy();
  });
});
