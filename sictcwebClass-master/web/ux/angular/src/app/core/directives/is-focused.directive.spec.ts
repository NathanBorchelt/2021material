import { IsFocusedDirective } from './is-focused.directive';

describe('IsFocusedDirective', () => {
  it('should create an instance', () => {
    const directive = new IsFocusedDirective();
    expect(directive).toBeTruthy();
  });
});
