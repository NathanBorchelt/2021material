import { VinValidatorDirective } from './vin-validator.directive';

describe('VinValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new VinValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
